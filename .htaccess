<IfModule mod_rewrite.c>
    RewriteEngine On

    # Redirect all requests to the Laravel public folder
    # This ensures /public is not exposed in the URL
    RewriteCond %{REQUEST_URI} !^/public/
    RewriteRule ^(.*)$ public/$1 [L]
</IfModule>
